/**
 * @internal
 * @group Market Data
 * @category HTTP
 */
export enum MarketDataRealTimeSubscriptionEntityType {
  news = 'news',
  trade = 'trades',
  quote = 'quotes',
  bar = 'bars',
  orderbooks = 'orderbooks',
}
